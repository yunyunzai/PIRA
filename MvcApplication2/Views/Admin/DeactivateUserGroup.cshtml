@model MvcApplication2.Models.UserGroup

<h3>Change activation status</h3>
<fieldset>
    <legend></legend>

    <div class="display-label">
         <b>@Html.DisplayNameFor(model => model.Name)</b>
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Name)
    </div>

    <div class="display-label">
         <b>@Html.DisplayNameFor(model => model.Abbreviate)</b>
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Abbreviate)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" name="deactivateUserGroup" value="Deactivate" /> |
        @Html.ActionLink("Cancel", "UserGroup")
    </p>
}